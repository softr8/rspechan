%h1 Latest Activity
%table{class: 'table table-bordered table-striped'}
  %tr
    %th Project
    %th Status
    %th
    %th Build
  - @feeds.each do |project|
    %tr
      %td= project.name
      %td= project.last_build.state.to_s.titleize
      %td= link_to 'Show', project_path(project)
      %td= link_to 'BUILD', enqueue_project_builds_path(project)
%br/
