!!!
%html
  %head
    %title RspecHan Continuous Integration Server
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .navbar
      .navbar-inner
        .container
          .span12
            %h1= link_to 'Continuous Integration Server', root_url
    .container
      .container-fluid
        .row-fluid
          .span2
            %h2 Projects
            %ul
            - @active_projects.each do |p|
              %li= link_to p.name, project_path(p.id)
            %h3= link_to 'New Project', new_project_path
            %h3= link_to 'Nodes', nodes_path
          .span10
            = yield